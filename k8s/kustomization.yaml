apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

resources:
  - serviceaccount.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - servicemonitor.yaml

# Override the image tag for a specific release:
# images:
#   - name: <your-registry>/oci-prometheus-exporter
#     newTag: "0.1.0"

# Override the compartment ID and region without editing configmap.yaml:
# configMapGenerator:
#   - name: oci-exporter-config
#     behavior: replace
#     files:
#       - config.yaml=overlays/prod/config.yaml